# Redis local configuration
daemonize no
port 6379
bind 127.0.0.1

# Data directory
dir ./redis/data

# Persistence
dbfilename dump.rdb
appendonly yes
appendfilename "appendonly.aof"

# Logging
loglevel notice

# Memory
maxmemory 256mb
maxmemory-policy allkeys-lru
