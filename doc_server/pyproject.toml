[project]
name = "mmdash-doc-server"
version = "0.1.0"
description = "数模Dashboard - 本地文档 Provider 服务"
requires-python = "==3.11.*"
dependencies = [
    "fastapi==0.115.0",
    "uvicorn[standard]==0.32.0",
    "pydantic-settings==2.6.1",
    "python-dotenv==1.0.1",
]

[tool.uv]
default-groups = []
